import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  navbar: {
    background: "#20272D",
    color: "white",
    padding: "1rem 1rem",
  },
}));

export default useStyles;
